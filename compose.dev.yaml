services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATABASE_PATH=/data/site.db
      - APP_ENV=development
      - LOG_LEVEL=debug
    volumes:
      - ./data:/data
    restart: unless-stopped
